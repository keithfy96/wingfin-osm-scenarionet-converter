# Stage 1 Main Reset

## User goal

Preserve the existing Stage 2 implementation on a local archival branch and
reset local `main` to a clean, working Stage 1 baseline in a sibling worktree.

## Run timestamp

2026-08-04 17:00:57 +08 (Asia/Singapore)

## Actions taken

- Created `legacy-stage2-implementation` at `37eccf5` in the original checkout.
- Created the sibling `converter-scenarionet-stage2-redesign` worktree on local
  `main`.
- Removed Stage 2-5 commands, implementation, tests, contract, configuration,
  dependencies, fixtures, and obsolete current guides.
- Kept Stage 1 acquisition, normalization, audit, and four inspection views.
- Copied only the ignored mosque source snapshot, manifest, and screenshots as
  redesign references.
- Rewrote current documentation to state the Stage 1 implementation boundary.

## Files and directories created or modified

- Modified Stage 1 CLI, configuration, inspector, audit wording, tests,
  dependency metadata, README, project guide, and public-driving policy.
- Removed downstream source modules, tests, configuration, fixture placeholders,
  implementation plans, and downstream guides.
- Added this action log.
- Preserved all prior `docs/ai-action-logs/` files unchanged.

## Commands and tools run

- Git branch/worktree creation and baseline file restoration.
- `uv lock`
- `uv run pytest -q`
- `uv run ruff check .`
- `git diff --check`
- CLI help and removed-command checks.
- Fixture-based `fetch` plus `source`, `normalized`, `audit`, and `stage-1`
  inspection runs.
- Repository-wide stale downstream-reference search.

## What worked

- The full suite passed: 22 tests.
- Ruff and whitespace validation passed.
- Fixture acquisition, normalization, audit, all four inspection views, and
  required artifact checks passed.
- CLI help lists only `fetch` and `inspect`; all removed commands return exit 2
  with `No such command`.

## What went wrong

- The first combined verification batch timed out while the new virtual
  environment downloaded dependencies. A standalone rerun completed after the
  downloads and produced the passing test result.

## Current state

Local `main` is a Stage-1-only redesign pending the reset commit. The original
checkout remains on the archival branch at the preserved implementation commit.
No remote branch was changed.

## Generated Code Details

### What was created or changed

The CLI and shared inspector were narrowed to Stage 1, downstream configuration
and dependencies were removed, and tests now enforce the smaller command and
inspection surface.

### Why it was created or changed

The redesign needs a verified Stage 1 foundation with no inherited downstream
implementation or placeholders.

### How it works

`fetch` builds and audits the Stage 1 workspace. `inspect` renders only
`source`, `normalized`, `audit`, or combined `stage-1` views. Pydantic rejects
removed downstream configuration because the model continues to forbid extra
fields.

### How it was validated

Validated by 22 unit tests, Ruff, `git diff --check`, CLI contract checks, and a
fixture-based end-to-end run of acquisition, normalization, audit, and every
supported inspection view.

## Recommended next step

Design the replacement post-Stage-1 workflow from this clean baseline in a new,
reviewable change.
