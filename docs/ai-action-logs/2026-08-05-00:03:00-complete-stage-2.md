# Complete Stage 2

- User goal: Carry out and complete Stage 2 of the direct OSM-to-ScenarioNet plan.
- Run timestamp: 2026-08-05 00:03:00 +08 (Asia/Singapore)

## Actions Taken

- Added deterministic topology-aware connector generation and movement classification.
- Added node-via restriction enforcement and proof-gated via-way enforcement.
- Added approaching-lane signal association and inferred stop-line candidates.
- Expanded the lane-model schema, generation report counts, and read-only map overlays.
- Added topology and integration tests and marked the Stage 2 checklist complete.

## Files and Directories Created or Modified

- `src/osm_scenario/generation.py`
- `src/osm_scenario/lane_model.py`
- `src/osm_scenario/topology.py`
- `tests/unit/test_generation.py`
- `tests/unit/test_topology.py`
- `docs/implementation-plan/README.md`
- `docs/ai-action-logs/2026-08-05-00:03:00-complete-stage-2.md`

## Commands and Tools

- Ran focused Stage 2 and CLI tests.
- Ran the full pytest suite, Ruff lint, Ruff format check, and `git diff --check`.
- Used the legacy Stage 2 branch only as an algorithmic reference.

## What Worked

- All 38 tests pass and Ruff lint passes.
- Generated artifacts remain deterministic across repeated runs.
- Ambiguous connectors remain review-required instead of being silently activated.
- Restricted connectors remain inspectable but are excluded from active lane links.

## What Went Wrong

- The first connector-curve test exposed the shared-endpoint case; the legacy-compatible
  short connector fallback was restored.
- The repository-wide Ruff format check reports six pre-existing unformatted files outside
  this change. All Stage 2 files changed in this run are formatted.
- No checked-in `workspaces/mosque` artifacts exist in this checkout, so validation used the
  normalized test workspace rather than a mosque smoke run.

## Current State

Stage 2 is implemented and its exit criteria are checked in the implementation plan. The
`generate-map` command writes the version-2 preliminary lane model, generation report,
read-only inspection HTML, and manifest checksums.

## Generated Code Details

- What changed: Added typed connector and stop-line features, restriction outcomes,
  connector geometry/movement helpers, topology resolution, overlays, and tests.
- Why: The foundation did not yet create legal intersection topology or traffic-control
  geometry, so Stage 2 could not be considered complete.
- How it works: Candidate movements come from projected incoming/outgoing lane topology,
  are filtered by OSM turn permissions, classified by signed angle, resolved against
  restrictions, and serialized with deterministic string IDs. Signals associate with
  approaching lanes and produce review-required stop lines 2 m upstream.
- Validation: 38 tests passed; Ruff lint passed; changed files are formatted; diff whitespace
  validation passed.

## Recommended Next Step

Run `generate-map` on the mosque workspace, inspect the generated overlays, and then begin
Stage 3 decision-capture work without manually drawing geometry.

## Continuation - 2026-08-05 00:30:26 +08

- Corrected connector candidate explosion found in the mosque smoke output.
- Ordinary same-way segment continuations now use direct lane links instead of connectors.
- Immediate reverse graph edges are excluded, and outgoing lanes are mapped by lane order
  rather than a full lane-to-lane cross product.
- Ambiguity is now limited to competing targets in the same movement family or borderline
  turn angles. Via-way proof accepts multiple lane connectors at one proven junction.
- Regenerated the mosque outputs with `direct-osm-stage2-v3`: connectors fell from 5,131 to
  787 and review-required connectors fell from 4,908 to 76. The result has zero same-way
  connectors and zero immediate reversals.
- Repeated mosque generation produced the same preliminary-model SHA-256
  (`d801e21ca5fe5fac1c50b9c27c26f1c053c27022755df5dd8b3f15903890dd74`).
- Validation: 40 tests passed, Ruff lint passed, changed files are formatted, and
  `git diff --check` passed.
- Correction to the earlier note: `workspaces/mosque` was present; the original shallow
  file search missed its nested artifacts. The mosque smoke run is now complete.

## Continuation - 2026-08-05 00:42:50 +08

- Replaced the way-ID intersection proxy with branch/control/restriction-based decision-node
  detection. A single continuation between different OSM ways is now linked directly.
- Added evidence-aware U-turn handling: explicit `reverse`/`uturn` lane tags permit a
  candidate, contradictory turn-lane tags exclude it, and an untagged plausible U-turn at
  a decision node remains review-required. `no_u_turn` relations still take precedence and
  mark generated candidates forbidden.
- Limited inferred U-turns to the innermost lane unless another lane has explicit evidence.
- Corrected left-driving `turn:lanes` ordering: OSM left-to-right tag positions are mapped
  to the generator's center-out lane indices.
- Added deterministic proportional lane mapping across lane-count changes plus a dedicated
  `lane_transition_count_mismatch` review finding.
- Regenerated all mosque Stage 2 artifacts with `direct-osm-stage2-v4`. The result contains
  1,857 lanes, 919 connectors (549 active, 327 review-required, 43 forbidden), 8 signal
  associations, and 19 inferred stop lines. Of the review-required connectors, 257 are
  deliberately surfaced untagged U-turn decisions rather than assumed legal movements.
- Verified that there are zero active reverse connectors without explicit source evidence,
  zero review-required reverse connectors outside the innermost lane, and zero reverse
  candidates outside a branch, traffic-control, or restriction node.
- Repeated generation produced the same preliminary-model SHA-256
  (`f22e32e97c3c17afb9828760eacfe114f4505e809eb16317c78c044bcb98b21f`).
- Validation: 47 tests passed, Ruff lint passed, and the mosque generation completed twice.

### Generated Code Details

- What changed: Decision-node classification, U-turn evidence policy, driving-side-aware
  turn-lane indexing, lane-count transition findings, and focused unit coverage.
- Why: The v3 count reduction incorrectly treated reverse movements as known-illegal and
  used OSM way boundaries as a proxy for real intersections.
- How it works: Physical branching, control nodes, and restriction-via nodes decide where
  connectors are needed; reverse movements then require positive tags for automatic
  activation and otherwise remain reviewable rather than silently legal or illegal.
- How it was validated: Full tests and lint passed; mosque invariants and deterministic
  regeneration were checked against the serialized preliminary lane model.

## Continuation - 2026-08-05 01:01:44 +08

- Added a Stage 2 visual review audit inspired by the legacy Stage 3A map while
  retaining the new direct-OSM lane model and review schema.
- The audit renders generated geometry on an OSM basemap, provides rule,
  severity, and free-text filters, and lists findings with source IDs, reasons,
  confidence, proposed values, and affected feature IDs.
- Clicking a finding focuses and highlights its mapped lanes, connectors, stop
  lines, or restriction-related geometry. Layer controls distinguish active,
  review-required, and forbidden connectors.
- Added `inspection/stage-2-review-audit.html` as a separate manifest-tracked
  artifact and upgraded `stage-2-map-review.html` to the same richer read-only
  visualization. Neither file captures decisions; that remains Stage 3 scope.
- Updated the Stage 2 policy and implementation plan with the new inspection
  artifact and its responsibility boundary.
- Mosque generation created a 3,282,250-byte self-contained-data HTML audit.
  A headless Chrome render confirmed the responsive panel, OSM basemap,
  generated layers, filter controls, review queue, and layer controls display.

### Generated Code Details

- What changed: The Stage 2 HTML renderer, artifact manifest entries, focused
  HTML tests, and operator documentation.
- Why: Text-only findings were difficult to evaluate without seeing the exact
  affected road geometry and surrounding map context.
- How it works: Projected model coordinates are converted to WGS84; findings
  carry derived `geometry_ids`; browser-side indexes connect those IDs to
  Leaflet layers for filtering, focus, and highlighting.
- How it was validated: Focused tests and Ruff passed, the mosque artifact was
  regenerated, required content markers were verified, and Chrome rendered a
  visual screenshot successfully.
