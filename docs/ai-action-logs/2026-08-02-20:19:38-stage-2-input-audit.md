# Stage 2 Input Audit

## User Goal

Generate the previously manual OSM data-quality analysis as repeatable JSON and
Markdown reports before Lanelet2 conversion.

## Run Timestamp

- Started: 2026-08-02 20:19:38 +08 (Asia/Singapore)

## Actions Taken

- Added exact OSM relation and relation-member parsing to the preserved source snapshot.
- Added an automatic Stage 2 input audit after Stage 1B normalization.
- Registered both audit reports in the Stage 1B manifest artifacts.
- Documented the reports and added fixture coverage for their contents and blocking state.
- Regenerated the mosque workspace reports from its preserved local OSM source.

## Files And Directories Created Or Modified

- `src/osm_scenario/stage2_input_audit.py`
- `src/osm_scenario/osm_source.py`
- `src/osm_scenario/normalization.py`
- `tests/unit/test_normalization.py`
- `README.md`
- `docs/implementation-plan/README.md`
- `docs/ai-action-logs/2026-08-02-20:19:38-stage-2-input-audit.md`

Generated ignored workspace artifacts:

- `workspaces/mosque/reports/stage-2-input-audit.json`
- `workspaces/mosque/reports/stage-2-input-audit.md`

## Commands Or Tools Run

- Re-ran `osm-scenario fetch` against `workspaces/mosque/source/map.osm` with the
  explicit left-driving setting and default versioned configuration.
- Ran the complete pytest suite, Ruff, and `git diff --check`.
- Inspected the generated mosque JSON, Markdown, and manifest entries.

## What Worked

- The generated mosque audit reproduced 227 retained ways, 100 missing lane counts,
  six graph components, eight source signals, 47 restrictions, and zero stop-line
  geometry candidates.
- Detailed JSON records make every aggregate traceable to source identifiers.
- Repeated Stage 1 execution replaces the reports without errors.
- All 19 tests and all static checks passed.

## What Went Wrong

- The first fixture assertion confused three source road segments with seven directed
  graph edges. The assertion was corrected to preserve the distinction explicitly.
- Ruff found three overlength lines in the new module; they were wrapped and rechecked.

## Current State

Stage 1B now generates and manifests a machine-readable Stage 2 readiness audit and
its concise Markdown summary. Stage 2 Lanelet2 generation remains unimplemented.

## Recommended Next Step

Review `workspaces/mosque/reports/stage-2-input-audit.md` and inspect detailed source
records in the JSON before implementing preliminary Lanelet2 generation.

## Generated Code Details

### What Was Created Or Changed

- Added source relation models and parsing.
- Added lane-count, width, connectivity, signal, restriction, and stop-line auditing.
- Added automatic report generation, manifest registration, and test coverage.

### Why It Was Created Or Changed

The earlier analysis used one-off scripts and could not be reproduced automatically
for each workspace. Stage 2 needs the same evidence before generating lane geometry.

### How It Works

The audit joins Stage 1 warnings and the normalized directed graph with exact nodes,
ways, tags, and relations from the preserved OSM XML. It writes aggregate counts and
complete identifier-level details to JSON, then derives a concise Markdown summary.

### How It Was Validated

- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q`: 19 passed.
- `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check .`: passed.
- `git diff --check`: passed.
- The regenerated mosque report exactly matched the previously observed totals.
