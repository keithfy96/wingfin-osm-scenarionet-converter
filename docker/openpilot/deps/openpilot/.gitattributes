# vendored into wingfin-osm-scenarionet-converter: no LFS server here, these are ordinary files
# *.dlc filter=lfs diff=lfs merge=lfs -text
# vendored into wingfin-osm-scenarionet-converter: no LFS server here, these are ordinary files
# *.onnx filter=lfs diff=lfs merge=lfs -text
